{
  "name": "watch-party",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "dependencies": {
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/prettier": "^3",
    "tsconfig": "*",
    "turbo": "latest",
    "typescript": "latest"
  },
  "packageManager": "yarn@4.0.0-rc.51",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
